# Upgrade Plan Template

Use this template for every concrete landing project.

## 1. Design Read

- Page kind:
- Audience:
- Offer:
- Primary CTA:
- Redesign mode: preserve / targeted evolution / overhaul
- Vibe:
- DESIGN_VARIANCE:
- MOTION_INTENSITY:
- VISUAL_DENSITY:
- Design system:

## 2. Current State

- What works:
- What feels weak:
- What must not change:
- Key files:
- Key assets:
- Current risks:
- Current quality score:
- Presentation-readiness verdict:

## 3. Copy And Offer Audit

- Current promise:
- Primary CTA:
- Offer clarity:
- Proof available:
- Main objections:
- Copy changes:
- Claims that need evidence:

## 4. Reference Board

For each reference:

- URL:
- Source lane: direct/domain / adjacent/theme / workflow-flow / visual language / motion-component / CRO-UX evidence / anti-reference
- Access status: live / blocked automation / browser-manual only / premium-account gated / timeout / sitemap-only
- Why relevant:
- Closeness reason:
- Borrow:
- Transform:
- Do not copy:
- Section mapping:
- Risk:

Minimum:

- 3 direct/domain references.
- 3 visual/aesthetic references.
- 2 motion/component references.

## 5. Visual Direction

- Typography:
- Palette:
- Radius system:
- Layout rhythm:
- Imagery/3D direction:
- Icon family:
- Theme: light / dark / auto
- What makes it memorable:

## 6. Section-By-Section Upgrade Plan

For every section:

- Current section:
- New purpose:
- Layout:
- Visible visual result:
- Copy changes:
- Asset needed:
- Motion:
- Inspiration:
- Component source:
- Mobile behavior:
- Risk:
- QA check:

## 7. Animation Storyboard

Before writing this, fill `26-motion-reference-map.md`:

- source URL:
- pattern:
- trigger:
- visible result:
- borrow:
- transform:
- do not copy:
- library implication:
- reduced-motion fallback:
- mobile simplification:
- decision:

For each animation:

- Element:
- Trigger:
- Motion:
- Purpose: hierarchy / storytelling / feedback / state transition
- Library:
- Reduced motion fallback:
- Performance note:

## 8. Component And Asset Plan

- Existing components to keep:
- Components to refactor:
- New components:
- Libraries/components to install:
- If Motion Primitives is considered, create `29-motion-primitives-selection.md` with exact item, install command, dependencies, reduced-motion fallback, mobile simplification, decision and QA:
- If Magic UI is considered, create `30-magic-ui-selection.md` with exact item, install command, dependencies, reduced-motion fallback, mobile simplification, decision and QA:
- If Aceternity UI is considered, create `31-aceternity-ui-selection.md` with exact item/block, type, license/access, install command or reference-only status, dependencies, adaptation, reduced-motion fallback, mobile simplification, decision and QA:
- If Tailark is considered, create `32-tailark-section-selection.md` with exact item/block/page, kit/source, live source URL, preview URL, registry URL, install command or reference-only status, access/license, dependencies, adaptation, reduced-motion fallback, mobile simplification, task ID, change ID, decision and QA:
- If shadcnblocks is considered, create `33-shadcnblocks-selection.md` with exact block/component/page/template, source type, access, live source URL, registry URL, install command or reference-only status, license/access, dependencies, adaptation, reduced-motion fallback, mobile simplification, task ID, change ID, decision and QA:
- If Kibo UI, Origin/Coss UI or another micro-component source is considered, create `34-micro-component-selection.md` with exact source/item, source URL, registry/copy URL, install command or copy method, dependencies, license/access, adaptation, reduced-motion fallback, mobile simplification, keyboard/focus QA, task ID, change ID, decision and QA:
- If React Bits is considered, create `35-react-bits-selection.md` with exact item, category, `TS-TW` variant, source URL, registry URL, install command, dependencies, license/access, heavy runtime gate, reduced-motion fallback, mobile simplification, task ID, change ID, decision and QA:
- If PaceKit GSAP is considered, create `36-pacekit-gsap-selection.md` with exact item, source URL, registry URL, install command, `gsap`/`@gsap/react` dependencies, registry dependencies, GSAP purpose, runtime gate, reduced-motion fallback, mobile simplification, task ID, change ID, decision and QA:
- If Cult UI is considered, create `37-cult-ui-selection.md` with exact item, source URL, registry URL, install command, dependencies, registry dependencies, visible purpose, adaptation, style/runtime gate, reduced-motion fallback, mobile simplification, task ID, change ID, decision and QA:
- If ReUI is considered, create `38-reui-selection.md` with exact item, style, source URL, registry URL, install command, free/pro access, license key status, dependencies, registry dependencies, adaptation, mobile behavior, keyboard/focus QA, task ID, change ID, decision and QA:
- If 21st.dev is considered, create `39-twenty-first-dev-selection.md` with exact component page, author/item, command, CDN registry URL or reference-only, license, dependencies, adaptation, fallback, mobile behavior, task ID, change ID, decision and QA:
- If Kokonut UI is considered, create `40-kokonut-ui-selection.md` with exact docs page, registry item, registry URL, install command, MIT/pro access, dependencies, registry dependencies, adaptation, reduced-motion fallback, mobile behavior, task ID, change ID, decision and QA:
- If MVPBlocks is considered, create `41-mvpblocks-selection.md` with exact docs page, exact item, endpoint URL, CLI or exact URL command, BSD-3-Clause/MIT license caveat, dependencies, registry dependencies, adaptation, reduced-motion fallback, mobile behavior, task ID, change ID, decision and QA:
- If SmoothUI is considered, create `42-smoothui-selection.md` with exact docs or preview page, exact item, endpoint URL, CLI/namespace/exact URL command, MIT license, dependencies, registry dependencies, API/registry evidence, adaptation, reduced-motion fallback, mobile behavior, task ID, change ID, decision and QA. Do not use `/api/v1/blocks` as block evidence:
- If HextaUI is considered, create `43-hextaui-selection.md` with exact item, source URL, endpoint URL, namespace/exact URL command, MIT license, dependencies, registry dependencies, HTML-doc caveat, demo-data replacement, mobile behavior, task ID, change ID, decision and QA:
- If Skiper UI is considered, create `44-skiper-ui-selection.md` with exact item, source URL, endpoint URL, namespace/exact URL command, terms/access status, dependencies, registry dependencies, premium/private material avoided, reduced-motion fallback, mobile behavior, task ID, change ID, decision and QA:
- If Eldora UI is considered, create `45-eldora-ui-selection.md` with exact item, item type, source URL, endpoint URL, namespace/exact URL command, MIT license, dependencies, registry dependencies, demo/example material avoided, reduced-motion fallback, mobile behavior, performance risk, task ID, change ID, decision and QA:
- If Blocks.so is considered, create `46-blocks-so-selection.md` with exact item, category, source page, endpoint URL, namespace/exact URL command, MIT license, duplicate-entry caveat, dependencies, registry dependencies, demo-data replacement, fake-proof handling, accessibility notes, mobile behavior, task ID, change ID, decision and QA:
- If Intent UI is considered, create `47-intent-ui-selection.md` with exact item, item type, source URL, endpoint URL, namespace/exact URL command, MIT license, React Aria dependency impact, dependencies, registry dependencies, example-page and `all` avoidance, demo-data replacement, accessibility notes, mobile behavior, task ID, change ID, decision and QA:
- Images needed:
- Video/3D needed:
- Icons/logos needed:

## 9. Implementation Tasks

Before writing implementation tasks, fill `27-responsive-viewport-map.md`:

- viewport set:
- first viewport plan:
- section transformations:
- typography and UI fit:
- asset crop rules:
- overflow and sticky risks:
- screenshot QA matrix:

Order tasks by build graph and risk:

1. Audit and tokens.
2. Hero.
3. Main conversion sections.
4. Proof/FAQ/CTA.
5. Motion layer.
6. Mobile pass.
7. Accessibility pass.
8. Performance pass.
9. Final visual QA.

For each task:

- files:
- action:
- expected visual change:
- verification:

Also create `20-implementation-task-graph.md` with stable task IDs, build rings, blockers, files/routes, visible result, verification and evidence.

Then create `28-change-traceability-matrix.md`:

- change IDs:
- source evidence:
- problem/reason:
- reference/brand/section decision:
- visible result:
- task ID:
- QA/evidence method:
- orphan or blocked decisions:

## 10. Quality Gate

Copy `05-quality-gate.md` and fill it honestly.
